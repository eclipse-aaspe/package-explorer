<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
             xmlns:cz="clr-namespace:FunctionZero.Maui.Controls;assembly=FunctionZero.Maui.Controls"
             xmlns:local="clr-namespace:MauiTestTree"
             x:Class="MauiTestTree.MainPage"
             Title="My2App">

    <ContentPage.Resources>
        <local:InverseBoolConverter
            x:Key="MyInverseBoolConverter" />

        <local:EnumToIntConverter
            x:Key="MyEnumToIntConverter" />
    </ContentPage.Resources>

    <!-- Navbar in IDTA blue, if dark theme -->
    <Shell.BackgroundColor>
        <AppThemeBinding
            Light="{StaticResource Gray100}"
            Dark="{StaticResource Primary}" />
    </Shell.BackgroundColor>
    
    <!-- For menues to work, it has to be a navigation page; per default this is given by
         having the shell for the application. -->
    <!-- The classic menu is for Windows and macOS Catalyst options. Not shown on any
         mobile devices -->
    <ContentPage.MenuBarItems>
        <MenuBarItem Text="App">
            <MenuFlyoutItem Text="Count" Clicked="ClassicMenuBarItem_Clicked"/>
        </MenuBarItem>
        <MenuBarItem Text="YouTube">
            <MenuFlyoutItem Text="Count"/>
            <MenuFlyoutSeparator/>
            <MenuFlyoutSubItem Text="You may want ..">
                <MenuFlyoutItem Text="Subscribe"/>
                <MenuFlyoutItem Text="Un-Subscribe"/>
            </MenuFlyoutSubItem>
        </MenuBarItem>
    </ContentPage.MenuBarItems>

    <!-- The Shell/ Navigation bar renders a top bar on mobile devices. 
         This contains multiple items (extra ordinary) on the left side.-->
    <Shell.TitleView>
        <Grid ColumnDefinitions="Auto,*,Auto">
            <!-- IDTA logo or AAS/ AssetIds -->
            <local:OrgOrAasLabel 
                Grid.Row="0" Grid.Column="0" 
                IsVisible="{Binding IsScreenIdiomDesktop, Converter={StaticResource MyInverseBoolConverter}}"
                WidthRequest="{Binding ScreenIdiom, Converter={StaticResource MyEnumToIntConverter}, ConverterParameter=220|180|140}" 
                AasId="{Binding AasId}" AssetId="{Binding AssetId}"/>

            <!-- Just a foreach-loop over the given items -->
            <!-- Note: For phones this should be empty! -->
            <HorizontalStackLayout x:Name="TitleViewMenuItems" HeightRequest="24"
                Grid.Row="0" Grid.Column="1"
                IsVisible="{Binding IsScreenIdiomDesktop, Converter={StaticResource MyInverseBoolConverter}}"
                VerticalOptions="Center"
                Margin="20,0,0,0" Spacing="8"
                BindableLayout.ItemsSource="{Binding MobileTopRootMenuHandles}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Button Clicked="OnTitleViewButton_Clicked"
                                Text="{Binding .}" 
                                Margin="0"
                                Padding="8,2,8,2" BackgroundColor="Transparent" CornerRadius="6"
                                BorderColor="White" BorderWidth="1.5"
                                TextColor="White" FontAttributes="Bold" FontSize="14"/>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </HorizontalStackLayout>

            <HorizontalStackLayout x:Name="TitleViewFixItems"
                Grid.Row="0" Grid.Column="2"
                VerticalOptions="Center"
                Margin="8,0,0,0" Spacing="8">

                <!-- Move current element up/ down for quick element transition -->
                <Button x:Name="MoveUpToolBarButton"
                    Clicked="ToolBarItem_Clicked" Padding="0" Margin="0"
                    BorderColor="Transparent" BorderWidth="0"
                    Background="Transparent">
                    <Button.ImageSource>
                        <FontImageSource
                            FontFamily="MaterialOutlined"
                            Glyph="{x:Static m:MaterialOutlined.Move_up}"
                            Size="24">
                            <FontImageSource.Color>
                                <AppThemeBinding
                                    Light="{StaticResource ToolbarIconColorLight}"
                                    Dark="{StaticResource ToolbarIconColorDark}" />
                            </FontImageSource.Color>
                        </FontImageSource>
                    </Button.ImageSource>
                </Button>

                <Button x:Name="MoveDownToolBarButton"
                    Clicked="ToolBarItem_Clicked" Padding="0" Margin="0"
                    BorderColor="Transparent" BorderWidth="0"
                    Background="Transparent">
                    <Button.ImageSource>
                        <FontImageSource
                            FontFamily="MaterialOutlined"
                            Glyph="{x:Static m:MaterialOutlined.Move_down}"
                            Size="24">
                            <FontImageSource.Color>
                                <AppThemeBinding
                                    Light="{StaticResource ToolbarIconColorLight}"
                                    Dark="{StaticResource ToolbarIconColorDark}" />
                            </FontImageSource.Color>
                        </FontImageSource>
                    </Button.ImageSource>
                </Button>

                <!-- Resize active panels for quick access elements/ element attributes -->
                <Button x:Name="LeftPanelToolBarButton"
                    Clicked="ToolBarItem_Clicked" Padding="0" Margin="0"
                    BorderColor="Transparent" BorderWidth="0"
                    Background="Transparent">
                    <Button.ImageSource>
                        <FontImageSource
                            FontFamily="MaterialOutlined"
                            Glyph="{x:Static m:MaterialOutlined.Left_panel_open}"
                            Size="24">
                            <FontImageSource.Color>
                                <AppThemeBinding
                                    Light="{StaticResource ToolbarIconColorLight}"
                                    Dark="{StaticResource ToolbarIconColorDark}" />
                            </FontImageSource.Color>
                        </FontImageSource>
                    </Button.ImageSource>
                </Button>

                <Button x:Name="RightPanelToolBarButton"
                    Clicked="ToolBarItem_Clicked" Padding="0" Margin="0"
                    BorderColor="Transparent" BorderWidth="0"
                    Background="Transparent">
                    <Button.ImageSource>
                        <FontImageSource
                            FontFamily="MaterialOutlined"
                            Glyph="{x:Static m:MaterialOutlined.Right_panel_open}"
                            Size="24">
                            <FontImageSource.Color>
                                <AppThemeBinding
                                    Light="{StaticResource ToolbarIconColorLight}"
                                    Dark="{StaticResource ToolbarIconColorDark}" />
                            </FontImageSource.Color>
                        </FontImageSource>
                    </Button.ImageSource>
                </Button>

            </HorizontalStackLayout>
        </Grid>
    </Shell.TitleView>

    <!-- Primary toolbar items are shown on the right of the top bar. -->
    <!-- Secondary toolbar items are shown below the hamburger menu. -->
    <!--
    <ContentPage.ToolbarItems>
        Move current element up/ down for quick element transition
        <ToolbarItem Text="MoveUp" Order="Primary" Clicked="ToolBarItem_Clicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="MaterialOutlined"
                    Glyph="{x:Static m:MaterialOutlined.Move_up}"
                    Size="24">
                    <FontImageSource.Color>
                        <AppThemeBinding
                            Light="{StaticResource ToolbarIconColorLight}"
                            Dark="{StaticResource ToolbarIconColorDark}" />
                    </FontImageSource.Color>
                </FontImageSource>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>

        <ToolbarItem Order="Primary" Clicked="ToolBarItem_Clicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="MaterialOutlined"
                    Glyph="{x:Static m:MaterialOutlined.Move_down}"
                    Size="24">
                    <FontImageSource.Color>
                        <AppThemeBinding
                            Light="{StaticResource ToolbarIconColorLight}"
                            Dark="{StaticResource ToolbarIconColorDark}" />
                    </FontImageSource.Color>
                </FontImageSource>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>

        Resize active panels for quick access elements/ element attributes
        <ToolbarItem Order="Primary">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="MaterialOutlined"
                    Glyph="{x:Static m:MaterialOutlined.Left_panel_open}"
                    Size="24">
                    <FontImageSource.Color>
                        <AppThemeBinding
                            Light="{StaticResource ToolbarIconColorLight}"
                            Dark="{StaticResource ToolbarIconColorDark}" />
                    </FontImageSource.Color>
                </FontImageSource>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>

        <ToolbarItem Order="Primary">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="MaterialOutlined"
                    Glyph="{x:Static m:MaterialOutlined.Right_panel_open}"
                    Size="24">
                    <FontImageSource.Color>
                        <AppThemeBinding
                            Light="{StaticResource ToolbarIconColorLight}"
                            Dark="{StaticResource ToolbarIconColorDark}" />
                    </FontImageSource.Color>
                </FontImageSource>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>

        will be replaced dynamically
        <ToolbarItem Text="File" Order="Secondary" Clicked="ToolBarItem_Clicked" />
        <ToolbarItem Text="Workspace" Order="Secondary" Clicked="ToolBarItem_Clicked" />
        <ToolbarItem Text="Option" Order="Secondary" Clicked="ToolBarItem_Clicked" />
        <ToolbarItem Text="Help" Order="Secondary" Clicked="ToolBarItem_Clicked" />
    </ContentPage.ToolbarItems>
    -->

    <!-- The main grid consists of a column left (elements) and a column right (element attributes)
         and a divider (between). -->
    <Grid x:Name="MainGrid" ColumnDefinitions="300,8,*" RowDefinitions="*">
        <!-- Left Panel -->
        <Grid RowDefinitions="Auto, *"
            x:Name="LeftContent"
            Grid.Row="0" Grid.Column="0">

            <Grid.Resources>
                <ControlTemplate x:Key="ElementChevronControlTemplate">

                    <HorizontalStackLayout >
                        <!--
                            The ControlTemplate sets the TreeNodeSpacer BindingContext to "{Binding Source={RelativeSource TemplatedParent}}" for us,
                            i.e. a ListItemZero.
                            The TreeNodeSpacer walks up the visual-tree to find the containing TreeViewZero, to get the IndentMultiplier.
                            It then sets its WidthRequest to the IndentMultiplier * (ListItemZero.BindingContext.Indent - 1)
                        -->
                        <cz:TreeNodeSpacer />

                        <local:TreeElemChevron
                            Margin="10"
                            Glyph="&#xf04b;|FASolid|#000000"
                            IsExpanded="{TemplateBinding BindingContext.IsExpanded, Mode=TwoWay}" 
                            ShowChevron="{TemplateBinding BindingContext.ShowChevron, Mode=TwoWay}" 
                        />
                        <!--This is-a ContentPresenter that allows us to specify a BindingContext for the Content-->
                        <cz:TreeContentPresenter 
                            VerticalOptions="Fill" 
                            HorizontalOptions="Fill"   
                            BindingContext="{TemplateBinding BindingContext.Data, Mode=OneWay}"
                        />
                    </HorizontalStackLayout>
                </ControlTemplate>

                <Style x:Key="ElementTreeStyle" TargetType="cz:ListItemZero">
                    <Setter Property="VisualStateManager.VisualStateGroups" >
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">

                                <!-- BackgroundColor must have a value set or the other states cannot 'put back' the original color -->
                                <!-- I *think* this is due to a bug in MAUI because unappyling a Setter ought to revert to the original value or default -->
                                <VisualState x:Name="Normal" >
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Transparent" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="ItemFocused">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="DarkGray" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="AliceBlue" />
                                    </VisualState.Setters>
                                </VisualState>

                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </Setter>
                </Style>
            </Grid.Resources>

                <!-- <VerticalStackLayout Padding="10"> -->

                <!-- TreeView -->
                <!--
                <material:TreeView
                SelectionMode="Single"
                ItemsSource="{Binding Nodes}"
                IsExpandedPropertyName="IsExtended"
                x:Name="treeView">

                    <material:TreeView.ItemTemplate>
                        <DataTemplate>
                            <HorizontalStackLayout Spacing="5">
                                <Label Text="{Binding Name}" FontAttributes="Bold" />
                                <Label Text="{Binding Children.Count, StringFormat='({0})'}" />
                            </HorizontalStackLayout>

                            <Grid ColumnDefinitions="30,*"
                                  Margin="-40, 0, 0, 0"
                                  Padding="4">

                                <Label
                                    Text="▶"
                                    FontSize="12"
                                    VerticalTextAlignment="Center"
                                    IsVisible="{Binding HasChildren}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label"
                                     Binding="{Binding IsExpanded}"
                                     Value="True">
                                            <Setter Property="Text" Value="▼"/>
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>

                                <Label
                                    Grid.Column="1"
                                    Text="{Binding Name}"
                                    VerticalTextAlignment="Center" />
                            </Grid>

                        </DataTemplate>
                    </material:TreeView.ItemTemplate>

                </material:TreeView>
                -->

                <!--
                <Label
                    Text=">"
                    FontFamily="OpenSans"
                    FontSize="40" />

                <Image Aspect="Center" Margin="10">
                    <Image.Source>
                        <FontImageSource Glyph="&#xe5cd;" FontFamily="MaterialOutlined" Size="40" />
                    </Image.Source>
                </Image>

                <Image Aspect="Center" Margin="10">
                    <Image.Source>
                        <FontImageSource Glyph="&#xf04b;" FontFamily="FASolid" Size="40" />
                    </Image.Source>
                </Image>
                -->

            <local:OrgOrAasLabel 
                Grid.Row="0" Grid.Column="0" 
                Margin="8,8,8,8"
                IsVisible="{Binding IsScreenIdiomDesktop}"
                AasId="{Binding AasId}" AssetId="{Binding AssetId}"/>

                <cz:TreeViewZero x:Name="MyTree"
                    Grid.Row="1" Grid.Column="0" 
                    ItemsSource="{Binding RootNode}" 
                    ItemHeight="40" 
                    Background="White" 
                    IsRootVisible="False"
                    SelectionMode="Single"
                    SelectedItem="{Binding SelectedItem}"                                  
                    TreeItemControlTemplate="{StaticResource ElementChevronControlTemplate}"
                    ItemContainerStyle="{StaticResource ElementTreeStyle}"
                    >
                    <cz:TreeViewZero.TreeItemTemplate>
                            <cz:TreeItemDataTemplate ChildrenPropertyName="Children" IsExpandedPropertyName="IsExpanded">
                            <DataTemplate>
                                <Grid ColumnDefinitions="40, 2000">
                                    <local:AutoFitLabel 
                                        Text="{Binding Tag}"
                                        HeightRequest="24" Background="{Binding ColorTagBg}" TextColor="{Binding ColorTagFg}" 
                                        Padding="0"/>
                                    <Grid Grid.Column="1" ColumnDefinitions="*,*"
                                            HeightRequest="24" Background="{Binding ColorElemBg}">
                                        <Label Grid.Column="0" Margin="4,0,0,0" Text="{Binding Caption}" TextColor="{Binding ColorElemFg}" VerticalTextAlignment="Center" FontAttributes="Bold" />
                                        <Label Grid.Column="1" Margin="4,0,0,0" Text="{Binding Info}" TextColor="{Binding ColorElemFg}" VerticalTextAlignment="Center" />                                                
                                    </Grid>
                                </Grid>
                                    
                            </DataTemplate>
                        </cz:TreeItemDataTemplate>
                    </cz:TreeViewZero.TreeItemTemplate>
                </cz:TreeViewZero>

                <!--
                    <cz:ListViewZero 
                        ItemsSource="{Binding ListData}"
                        ItemHeight="40"
                        SelectionMode="Multiple">
                        <cz:ListViewZero.ItemTemplate>
                            <DataTemplate>
                                <Label Text="{Binding Name}" />
                            </DataTemplate>
                        </cz:ListViewZero.ItemTemplate>
                    </cz:ListViewZero>
                -->

        </Grid>

        <!-- Divider -->
        <BoxView
            x:Name="Divider"
            Grid.Row="0" Grid.Column="1" 
            BackgroundColor="{DynamicResource Gray100}"
            WidthRequest="8">
            <BoxView.GestureRecognizers>
                <PanGestureRecognizer PanUpdated="OnDividerPanUpdated"/>
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- Right Panel -->
        <!-- Left Panel -->
        <ContentView
            x:Name="RightContent"
            Grid.Row="0" Grid.Column="2">

            <ScrollView x:Name="RightScrollView">

                <VerticalStackLayout
                Padding="30,0"
                Spacing="25">

                    <Label
                    Text=">"
                    FontFamily="OpenSans"
                    FontSize="40"
                    TextColor="DarkGreen"/>

                    <Image Aspect="Center" Margin="10">
                        <Image.Source>
                            <FontImageSource Glyph="&#xe5cd;" FontFamily="MaterialOutlined" Size="40" Color="Black" />
                        </Image.Source>
                    </Image>

                    <Image Aspect="Center" Margin="10">
                        <Image.Source>
                            <FontImageSource Glyph="&#xf04b;" FontFamily="FASolid" Size="40" Color="Blue" />
                        </Image.Source>
                    </Image>

                    <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a hovercraft number nine" />

                    <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

                    <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                    <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

                </VerticalStackLayout>
            </ScrollView>
        </ContentView>
    </Grid>

</ContentPage>



----

IsVisible="{Binding IsScreenIdiomDesktop, Converter={StaticResource MyInverseBoolConverter}}"